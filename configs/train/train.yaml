batch_size: 128
num_workers: 0
max_epochs: 5
seed: 42

trainer:
  accelerator: "auto"
  devices: "auto"
  log_every_n_steps: 10

early_stopping:
  enabled: true
  monitor: "val/f1"
  mode: "max"
  patience: 2
